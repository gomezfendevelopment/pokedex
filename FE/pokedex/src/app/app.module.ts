// AppModule retained for backward-compatibility but not used.
// The application is now bootstrapped using standalone components
// via `bootstrapApplication` in `src/main.ts`.

export const AppModuleDeprecated = null as unknown as void;
